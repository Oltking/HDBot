# Copy this file to `.env` and fill in your real values. NEVER commit `.env`.
# Create a token at Deriv.com -> Account Settings -> API token (scopes: Read, Trade).

DERIV_API_TOKEN=your_token_here
DERIV_APP_ID=1089
